#include <iostream>
#include <string>
#include <cstdlib>
using namespace std;
int main()
{
    int n,i;
    string a;
    cin>>n;    
    getline(cin,a);
    if(a.substr(0,6)!="mumble" && s.substr(a.length()-6)=="mumble")
    {
        int pos = a.find("mumble");
        
    }
    
    return 0;
}